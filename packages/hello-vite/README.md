# hello-vite

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build hello-vite` to build the library.

## Running unit tests

Run `nx test hello-vite` to execute the unit tests via [Jest](https://jestjs.io).
